import { render, screen } from '@testing-library/react';
import { addCityToList } from '../../_lib/api';

describe('Async component', () => {
  test('renders a city if request succeeds', () => {
    render()
  });
});